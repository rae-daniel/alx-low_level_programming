#include <stdio.h>
/**
 * main - Entry point
 * Return: eachtime 0 (successfull)
 */

int main(void)
{
	char b;

	b = 'z';
	while
		(b >= 'a') {
			putchar(b);
			b--;
		}
	putchar('\n');
	return (0);
}
