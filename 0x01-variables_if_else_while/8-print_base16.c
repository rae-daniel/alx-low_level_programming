#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Successful)
 */

int main(void)
{
	char n;

	int m;

	n = 'a';
	m = 0;
	while
		(m < 10) {
			putchar(m + '0');
			m++;
		}
	while
		(n <= 'e') {
			putchar(n);
			n++;
		}
	putchar('\n');
	return (0);
}
